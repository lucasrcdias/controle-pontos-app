include _navbar.pug

.next-point-wrapper
  .container
    h2.next-point PRÓXIMO PONTO

    h1.next-point-time
      strong {{ nextPointTime }}

button.btn-main-action(ng-class="actionStatus", ng-click="savePoint()")
  i.fa.btn-icon(ng-class="actionIcons[actionStatus]")
